pipeline {
    agent {
  dockerfile {
    args '-p 8000:8085 --name \'miappjenkins\''
    dir '.'
    filename 'Dockerfile'
    reuseNode true
  }
}
    stages {
        stage('Build') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Test') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Deploy') {
            steps {
                sh 'python --version'    
            }
        }
    }
}
