pipeline {
    agent none
    
//tools{
  //  dockerTool 'docker'
//}
    stages {
        stage('Build') {
            agent {
            //dockerfile true
            dockerfile {
            //additionalBuildArgs  '-t mi_imagen:${BUILD_NUMBER}'
            additionalBuildArgs  '--build-arg \'-t mi_imagen:${BUILD_NUMBER}\''
            //args '--name \'micontainer\' -p 9000:8085 '
            //args '-p 9000:8085 '
            /*dir '.'
            filename 'Dockerfile'*/ 
            reuseNode true
            }
            }
            steps {
                sh 'python --version'    
            }
        }
         stage('Test') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Deploy') {
            steps{
             node {
                labe 'docker'
                //dockerfile true
                //additionalBuildArgs  '-t mi_imagen:${BUILD_NUMBER}'
                //additionalBuildArgs   '--build-arg \'-t mi_imagen:${BUILD_NUMBER}\''
                args '--name \'micontainer\' -p 9000:8085 mi_imagen:${BUILD_NUMBER}'
                //args '-p 9000:8085 '
                /*dir '.'
                */ 

                }
            }
        }
    }
}
