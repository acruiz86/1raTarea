pipeline {
   agent{ dockerfile {
    additionalBuildArgs  '-t mi_imagen:${BUILD_NUMBER} --no-cache'
    args '--name alll -p 9000:8085'
            //dockerfile true
            //dockerfile {

            //additionalBuildArgs  '-t mi_imagen:${BUILD_NUMBER}'
            
            //args '-p 9000:8085 '
            /*dir '.'
            filename 'Dockerfile'*/ 
            //reuseNode true
   }
            }
            
    
//tools{
  //  dockerTool 'docker'
//}
    stages {
        stage('Build') {            
            steps {
                echo "dfsfsdfsff"
            }
        }
         stage('Test') {
            steps {
            echo "dfdsfrdsff fsdf"       
            }
        }
         stage('Deploy') {
            steps{
             echo "dfdsfdsfsdf"   
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
