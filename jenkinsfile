pipeline {
    agent {
        //dockerfile true
 dockerfile {
additionalBuildArgs  '-t mi_imagen:${BUILD_NUMBER}'
//additionalBuildArgs  '--build-arg \'-t mi_imagen:${BUILD_NUMBER}\''
//args '--name \'micontainer\' -p 9000:8085 '
    /*dir '.'
    filename 'Dockerfile'
    reuseNode true
  */}
}
tools{
    dockerTool 'docker'
}
    stages {
        stage('Build') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Test') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Deploy') {
             steps {
                script{
                //sh 'docker run --name container${BUILD_NUMBER} -p 9000:8085 mi_imagen:${BUILD_NUMBER}'
                echo " Desplegado ............ docker ....correctamente"   
            }
            }
        }
    }
}
