pipeline {
    agent {
        //dockerfile true
 dockerfile {
additionalBuildArgs  '-t mi_imagen:${BUILD_NUMBER}'
//additionalBuildArgs  '--build-arg \'-t mi_imagen:${BUILD_NUMBER}\''
args ' -d --name \'micontainer\' -p 8000:8085'
    /*dir '.'
    filename 'Dockerfile'
    reuseNode true
  */}
}
    stages {
        stage('Build') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Test') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Deploy') {
            steps {
                sh 'docker run -d -p 8080:8085 mi_imagen:${BUILD_NUMBER}' 
                echo " Desplegado ...........correctamente"   
            }
        }
    }
}
