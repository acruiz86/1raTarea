pipeline {
    agent {
        //dockerfile true
 dockerfile {
additionalBuildArgs  '-t mi_imagen:${BUILD_NUMBER}'
//additionalBuildArgs  '--build-arg \'-t mi_imagen:${BUILD_NUMBER}\''
//args '--name \'micontainer\' -p 9000:8085 '
    /*dir '.'
    filename 'Dockerfile'
    reuseNode true
  */}
}
    stages {
        stage('Build') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Test') {
            steps {
                sh 'python --version'    
            }
        }
         stage('Deploy') {
            steps {
                sh 'run --name container${BUILD_NUMBER} -d -p 9000:8085 mi_imagen:${BUILD_NUMBER}'
                echo " Desplegado ......... docker ....correctamente"   
            }
        }
    }
}
